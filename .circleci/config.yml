version: 2
jobs:
  build:
    macos:
      docker:
        - image: circleci/ruby:2.4.1
      xcode: "9.2.0"
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: bundle install
      - run:
          name: Run Danger
          command: bundle exec danger
          when: always
